[build]
target = "index.html"
dist = "dist"
release = false
# Capsule serves itself relative to its mount path (e.g. /apps/hey-social/);
# matches the same trick used in client/vite.config.js for hey-social.
public_url = "./"

[serve]
addresses = ["127.0.0.1"]
port = 8080
open = false
